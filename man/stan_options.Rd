% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backends.R
\name{stan_options}
\alias{stan_options}
\title{Stan Sampler Options}
\usage{
stan_options(..., chains = 4L, backend = "rstan")
}
\arguments{
\item{...}{sampler arguments forwarded verbatim to the chosen backend's
sampler. Use the backend's own names: for \code{"rstan"}, the
\code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}} arguments (\code{iter}, \code{cores}, \code{seed}); for
\code{"cmdstanr"}, the \verb{$sample()} arguments (\code{iter_warmup}, \code{iter_sampling},
\code{parallel_chains}, ...).}

\item{chains}{A positive integer specifying the number of Markov chains. 
    The default is 4.}

\item{backend}{which Stan interface to target, one of \code{"rstan"} (default) or
\code{"cmdstanr"}. Determines which argument vocabulary is accepted and which
sampler \code{\link[=fit_model]{fit_model()}} calls. Selecting \code{"cmdstanr"} errors if the cmdstanr
package is not installed.}
}
\value{
a named list of validated sampler arguments, carrying a \code{backend}
element recording the backend it was built for
}
\description{
Collects and validates sampler arguments for the chosen \code{backend}, forwarding
them \strong{verbatim} so calls feel native to that backend. Use the backend's own
argument names; mixing one backend's vocabulary into the other errors with a
hint. The model object is supplied separately (via \code{\link[=fit_model]{fit_model()}}), while
\code{data} and \code{init} are constructed internally, so none of these may be set
here. \code{chains} defaults to \code{4} so downstream code can always size per-chain
structures from it.
}
\examples{
stan_options()
stan_options(chains = 2, iter = 500)
if (requireNamespace("cmdstanr", quietly = TRUE)) {
  stan_options(backend = "cmdstanr", parallel_chains = 4, iter_warmup = 500)
}

}
