% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use-flexstanr.R
\name{useFlexStanR}
\alias{useFlexStanR}
\title{Wire flexstanr into a host package}
\usage{
useFlexStanR(path = ".", remote = "ACCIDDA/flexstanr", on_cran = FALSE)
}
\arguments{
\item{path}{path to the host package's root (the directory containing its
\code{DESCRIPTION}). Defaults to the working directory.}

\item{remote}{the GitHub \code{owner/repo} used for the interim \code{Remotes} entry
while flexstanr is pre-CRAN.}

\item{on_cran}{set \code{TRUE} once flexstanr is on CRAN to skip the interim
\code{Remotes} entry; a plain \code{Imports} dependency is then enough.}
}
\value{
the host package \code{path}, invisibly.
}
\description{
A one-time setup helper, in the spirit of usethis's \verb{use_*} functions, that
declares flexstanr as a dependency of the host package you run it from. It
adds flexstanr (and rstan, the default backend) to the host's \code{Imports} and,
until flexstanr is on CRAN, an interim \code{Remotes} entry so \code{remotes} / \code{pak}
can install it from GitHub.

No further wiring is needed: the host calls \code{\link[=fit_model]{fit_model()}} and the \verb{backend_*}
accessors directly, and flexstanr resolves the host's own compiled models
automatically from the calling package.
}
\examples{
\dontrun{
# from the root of your Stan package:
flexstanr::useFlexStanR()
}

}
