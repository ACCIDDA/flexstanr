% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backends.R
\name{cmdstanr_extract}
\alias{cmdstanr_extract}
\title{Reshape cmdstanr draws into rstan::extract()'s list-of-arrays}
\usage{
cmdstanr_extract(draws, pars)
}
\arguments{
\item{draws}{a posterior \code{draws} object for the requested parameters.}

\item{pars}{the parameter base names to extract.}
}
\value{
a named list of draw arrays, one per parameter.
}
\description{
Groups the flat, indexed variables (\code{theta[1]}, \code{theta[2]}, ...) back into one
array per parameter with draws merged across chains, matching the shape
\code{\link[rstan:stanfit-method-extract]{rstan::extract()}} returns: a bare vector for a scalar parameter, an
\verb{S x dims} array otherwise. Unlike rstan's default the draws are not randomly
permuted; they keep iteration-chain order, which is immaterial for the
exchangeable-sample uses these draws are put to.
}
\keyword{internal}
