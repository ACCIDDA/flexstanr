% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backends.R
\name{fit_model}
\alias{fit_model}
\title{Fit a Stan model through the chosen backend}
\usage{
fit_model(
  model_name,
  dat_stan,
  init,
  stan_opts,
  drop_pars = NULL,
  package = caller_package()
)
}
\arguments{
\item{model_name}{name of the Stan model; used to look up the compiled model
in the calling package's \code{stanmodels} (rstan) and to locate the \code{.stan}
source file under its \verb{inst/stan/} (cmdstanr).}

\item{dat_stan}{the Stan data list.}

\item{init}{the init list, sized to the chain count.}

\item{stan_opts}{the validated \code{\link[=stan_options]{stan_options()}} list (carrying a \code{backend}
element).}

\item{drop_pars}{character vector of parameter names to exclude from the
saved draws, or \code{NULL} to keep everything. Honored by rstan; cmdstanr
cannot drop parameters and warns if any are requested.}

\item{package}{name of the host package whose model is being fit. Defaults to
the package that called \code{fit_model()}, which is correct for the usual case
of a host package fitting one of its own models.}
}
\value{
the backend's fit object (a \code{stanfit} or \code{CmdStanMCMC}).
}
\description{
Dispatches a fit to the backend recorded on \code{stan_opts} (from
\code{\link[=stan_options]{stan_options()}}). The compiled model is resolved by \code{model_name} from the
calling package: for \code{"rstan"}, \code{package::stanmodels[[model_name]]}; for
\code{"cmdstanr"}, \verb{inst/stan/<model_name>.stan} under \code{package}. The calling
package is detected automatically and can be overridden with \code{package}.
}
\examples{
\dontrun{
# From inside a host package that ships a compiled `coverage` model:
opts <- stan_options(chains = 2, iter = 500, seed = 1)
fit <- fit_model("coverage", dat_stan = data_list, init = init_list,
                 stan_opts = opts)
}

}
