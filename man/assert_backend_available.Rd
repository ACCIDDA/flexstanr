% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backends.R
\name{assert_backend_available}
\alias{assert_backend_available}
\title{Assert a backend name is valid and its package is installed}
\usage{
assert_backend_available(backend)
}
\arguments{
\item{backend}{the backend to validate.}
}
\value{
the validated backend string, invisibly.
}
\description{
Validates \code{backend} against the known choices (so it also subsumes
\code{match.arg()}) and, for the optional cmdstanr backend, that its package is
installed. rstan is always available (a hard dependency); cmdstanr is
optional, so selecting it without the package installed fails early here
rather than deep inside the fit. Returns the validated backend invisibly.
}
\keyword{internal}
